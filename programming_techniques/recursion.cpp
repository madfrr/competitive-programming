#include <bits/stdc++.h>
using namespace std;

/*
Tentar fazer permutação e subsets de forma recursiva
backtracking tbm
*/

// Com manipulação de bits também é possível fazer operações de conjunto
// Um inteiro de 32 bits é possível armazenar um set com todos os valores entre 1 e 32. Sendo que cada bit (em sua posição) representa um nro desse set
// A std lib do c++ possui uma estrutura de bitset que da para armazenar os bits como se fossem um array

int main(){
    return 0;
};